"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
exports.__esModule = true;
exports.getCodepoints = void 0;
var constants_1 = require("../constants");
exports.getCodepoints = function (assets, predefined, start) {
    if (predefined === void 0) { predefined = {}; }
    if (start === void 0) { start = constants_1.DEFAULT_START_CODEPOINT; }
    var out = {};
    var used = Object.values(predefined);
    var current = start;
    var getNextCodepoint = function () {
        while (used.includes(current)) {
            current++;
        }
        var res = current;
        current++;
        return res;
    };
    for (var _i = 0, _a = Object.keys(assets); _i < _a.length; _i++) {
        var id = _a[_i];
        if (!predefined[id]) {
            out[id] = getNextCodepoint();
        }
    }
    return __assign(__assign({}, predefined), out);
};
//# sourceMappingURL=codepoints.js.map