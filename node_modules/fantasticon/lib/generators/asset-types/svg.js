"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
exports.__esModule = true;
var fs_1 = require("fs");
var svgicons2svgfont_1 = __importDefault(require("svgicons2svgfont"));
var generator = {
    generate: function (_a) {
        var fontName = _a.name, fontHeight = _a.fontHeight, descent = _a.descent, normalize = _a.normalize, assets = _a.assets, codepoints = _a.codepoints, _b = _a.formatOptions, svg = (_b === void 0 ? {} : _b).svg;
        return new Promise(function (resolve) {
            var font = Buffer.alloc(0);
            var fontStream = new svgicons2svgfont_1["default"](__assign({ fontName: fontName,
                fontHeight: fontHeight,
                descent: descent,
                normalize: normalize, log: function () { return null; } }, svg))
                .on('data', function (data) { return (font = Buffer.concat([font, data])); })
                .on('end', function () { return resolve(font.toString()); });
            for (var _i = 0, _a = Object.values(assets); _i < _a.length; _i++) {
                var _b = _a[_i], id = _b.id, absolutePath = _b.absolutePath;
                var glyph = fs_1.createReadStream(absolutePath);
                var unicode = String.fromCharCode(codepoints[id]);
                var ligature = '';
                for (var i = 0; i < id.length; i++) {
                    ligature += String.fromCharCode(id.charCodeAt(i));
                }
                glyph.metadata = { name: id, unicode: [unicode, ligature] };
                fontStream.write(glyph);
            }
            fontStream.end();
        });
    }
};
exports["default"] = generator;
//# sourceMappingURL=svg.js.map